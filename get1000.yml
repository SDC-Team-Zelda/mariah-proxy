
config:
  target: "http://localhost:3000"
  # processor: "./dataGen/stressSeeder.js"
  phases:
    - duration: 60
      arrivalRate: 1000

scenarios:
  - name: "GET host"
    flow:
      - get:
          url: "/"
          json:
            id: 450230
